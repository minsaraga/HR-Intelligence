services:
  backend:
    build: ./backend
    container_name: ai-hr-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: ai-hr-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
